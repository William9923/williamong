const dir = "./data"

const filename = {
    "user": `${dir}/user.json`,
    "account": `${dir}/account.json`,
    "contact": `${dir}/contact.json`,
    "experience": `${dir}/experience.json`,
    "projects": `${dir}/projects.json`,
}

export default filename;